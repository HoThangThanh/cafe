services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: cafe_backend
    ports:
      - "8080:8080"
    environment:
      - SPRING_DATA_MONGODB_URI=mongodb+srv://thanh99902_db_user:17UgHAMV9rFqk5vc@cluster0.h8ajqhd.mongodb.net/YOUR_DB_NAME?retryWrites=true&w=majority
      - SERVER_PORT=8080
    networks:
      - cafe-net

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: cafe_frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
    networks:
      - cafe-net

networks:
  cafe-net:
    driver: bridge
